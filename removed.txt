GestoreViaggi.java - 136

    private static ArrayList<PrenotazioneDAO> caricaPrenotazioniDaDB() throws DatabaseException {
        ArrayList<PrenotazioneDAO> listaDAOPrenotazioni;
        listaDAOPrenotazioni = PrenotazioneDAO.getPrenotazioni();
        return listaDAOPrenotazioni;
    }

EntityPrenotazione.java - 52

    public void popolaViaggio() throws DatabaseException {
        PrenotazioneDAO prenotazioneDAO = new PrenotazioneDAO(this.id);
        ViaggioDAO viaggioDAO = new ViaggioDAO(prenotazioneDAO.getIdViaggioPrenotato());
        this.viaggioPrenotato = new EntityViaggio(viaggioDAO);
    }